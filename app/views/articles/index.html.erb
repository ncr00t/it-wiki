<p id="notice"><%= notice %></p>

<h1><% if @articles.present? %> All Articles (<%= @articles.size %>) <% end %></h1>

<% @articles.each do |article| %>
  <h2><%= link_to article.title, article %></h2>
  <p><%= truncate(article.body) %></p>
<% end %>